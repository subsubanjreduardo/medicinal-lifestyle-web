// Startup applies migrations and imports the supplied snapshot only into an empty database.
console.log('Initial catalog setup happens automatically on first startup.');
console.log('Run npm run dev, then open http://localhost:3000.');
console.log('Existing products, orders and stock are never overwritten by this command.');
